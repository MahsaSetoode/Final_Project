import {BE_ADMIN} from '../action/action-types'
const initialState = {
    authenication: true,
}

export default (state = initialState, action) => {
    switch(action.type){
        // case BE_ADMIN:{
        //     return(
        //         // ...state,
        //         // // if(state.authenication === "admin"){
        //         // //    authenication: true, 
        //         // // }
        //         // authenication: !state.authenication, 
        //     )
        // }    
    }
}